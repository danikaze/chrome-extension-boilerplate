/**
 * Definition of all the data that can be stored locally using `IndexedDB`.
 * Note that each field will be stored atomically.
 *
 * Utilities to interact with the database are provided in [utils/storage.ts](./utils/storage.ts)
 */
export interface StorageData {}
